/**
 * Namespace containing enums related to commands.
 *
 * @namespace _Enums
 */
export namespace _Enums {
    /**
     * Enumeration of commands.
     *
     * @enum {string}
     * @readonly
     */
    export enum Commands {
        /**
         * Represents the 'generate' command. Expected this as an argument to the
         * 'dev.runner' what will represent that you want to perform operations
         * related to 'generate' namespace
         */
        Generate = "generate",
    }
}
