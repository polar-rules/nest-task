/**
 * Namespace containing types related to notes.
 * @namespace _Types
 */
export namespace _Types {
    /**
     * Options for controlling spacing in notes.
     *
     * @interface SpaceOptions
     * @property {boolean} space - Whether to include a space in the note.
     */
    export interface SpaceOptions {
        space: boolean;
    }
}
